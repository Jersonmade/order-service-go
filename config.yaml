server:
  port: ":8080"

database:
  url: "postgres://postgres_user:postgres_password@postgres:5432/wb_test_db?sslmode=disable"

kafka:
  brokers:
    - "kafka:9092"
  topic: "orders"
  group_id: "order-consumers"
  partition: 0
  min_bytes: 1
  max_bytes: 10000000

shutdown_timeout_seconds: 10
